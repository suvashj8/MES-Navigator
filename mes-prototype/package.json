{
  "name": "mes-worker-grading",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync:dev-env": "node scripts/sync-dev-env.mjs",
    "db:docker": "npm run sync:dev-env && docker compose --env-file .env.docker up -d mes-postgres",
    "db:docker:down": "docker compose --env-file .env.docker down",
    "docker:up": "docker compose --env-file .env.docker up -d --build",
    "docker:down": "docker compose --env-file .env.docker down",
    "docker:logs": "docker compose --env-file .env.docker logs -f",
    "docker:seed": "docker compose --env-file .env.docker exec mes-api node seed.js",
    "docker:reseed": "docker compose --env-file .env.docker exec mes-api node seed.js",
    "db:restore": "npm run sync:dev-env && npm run db:docker && npm run db:restore --prefix server",
    "dev": "npm run sync:dev-env && concurrently -n api,ui -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:ui": "npm run dev --prefix client",
    "dev:api": "npm run dev --prefix server",
    "seed": "npm run seed --prefix server",
    "sync:staff": "npm run sync:staff --prefix server",
    "sync:grading": "npm run sync:grading --prefix server",
    "db:setup": "npm run db:setup --prefix server",
    "migrate:from-sqlite": "npm run migrate:from-sqlite --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
